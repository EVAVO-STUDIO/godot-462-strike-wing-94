# Coastal watch FX v3

Four 640x272 exposures at 3 fps replace the oversized green radar arc and detached trails in end_consequence_final. Thin slate condensation ribbons begin behind the actual fighter exhaust positions (subject centre 390,164, scale .82; FX surface offset 0,24), gently bend, and fade into the coastal atmosphere. The painted background and aircraft cels are preserved.

EVAVO Art Studio finishes the retained SVG sources. The builder verifies every source and output hash before writing any runtime frame. Sprite Studio default strict checks pass; exposure 3 intentionally repeats exposure 1 for the returning bend. All four alpha checks and six-background proofs were reviewed, together with four native Godot cinematic exposures in the isolated art checkout. This is art registration evidence, not a full campaign, audio or release certification.

Main verification: pinned Godot 4.6.2 import completed without engine errors, campaign cinematic self-test passed (existing ObjectDB exit leak warning remains), and all four directly addressed main-build exposures were visually inspected. The initial 8.9-second wall-clock capture landed on the preceding city shot and is not watch-shot evidence. The direct fixture uses --capture-gameplay to bypass the startup overlay; it does not prove natural campaign triggers or final audio.
