# Machine War: shared-enemy composition

The drone in the control-room shot previously occupied screen position (320,174), over the console. It now occupies (320,110), beyond the window. Four smaller acquisition-marker cels are registered to that position, keeping all marks in the exterior view. EVAVO Art Studio finished the original SVG cels; all four pass native-alpha checks, and the six-background proofs for the widest and tightest states were inspected. Original runtime cels and cinematic data are preserved in `originals`.

The runtime change is four FX PNGs and one subject-position value. Shot duration, camera behavior, subject animation, captions, all three story arcs and all twelve plates are preserved. `tools/build_city_warning_v3.mjs` verifies every source and output hash before writing. The existing Machine War builder delegates this FX row to that builder so rebuilding cannot silently restore the misplaced markers.

All 48 current cinematic exposures have review coverage. The corrected city shot, refined re-entry and refined coastal-watch frames were viewed directly. The other 36 current frames are byte-identical to their individually inspected prior captures; `capture_review.json` records hashes and review provenance. The remaining plates retain the refinery, factory, atmospheric ascent, orbital infrastructure, Ark and recovering-city imagery. Small factory ignition dots remain visually simple; this review does not imply final sound or motion acceptance.

An initial refreshed capture was obscured by the startup logo and was rejected. The final fixture explicitly completes startup before capturing. Its native log contains no engine error or warning. The existing cinematic self-test passes with the known ObjectDB shutdown warning. The addressed fixtures do not verify natural campaign triggers, uninterrupted playback, skipping or audio transitions. The declared environmental/prelap/carry/silence bridges still require the later audio integration pass and mixed listening review.
