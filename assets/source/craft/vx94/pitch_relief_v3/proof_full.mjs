import {readFile,writeFile} from 'node:fs/promises';
const b='work/vx94_pitch_relief_v3/';let r=JSON.parse(await readFile(b+'board_request.json','utf8'));r.sandboxId='pitch-proof-board';r.tasks[0].sources=r.tasks[0].sources.map(s=>s.replace('/native/','/clean_proofs/'));r.tasks[0].targetPath='proof_board.png';r.tasks[0].canvas={width:960,height:704,background:'#17212b'};r.tasks[0].layers=r.tasks[0].sources.map((_,i)=>({sourceIndex:i,x:i%4*240,y:Math.floor(i/4)*176,sourceRect:{x:0,y:0,width:240,height:176},sampling:'nearest'}));await writeFile(b+'proof_full_request.json',JSON.stringify(r,null,2));
