# Retained destruction material: complete boss-family preview

The current renderer removes the living hull immediately and waits until 32% of its destruction event to begin drawing wreckage. Native comparisons show all nine bosses reduced to small fragments at 0.708 seconds, before their full hulls reappear. This extends the earlier Ark-only finding. The six smaller cases exercise naval, airframe, vehicle, mech and emplacement breakup families.

The candidate holds the existing zero-progress breakup pose beneath the initial blast, through event ratio 0.32 inclusive. Above that ratio, the existing consequence renderer takes over once, using its unchanged timing, drift, secondary explosions and fade. No replacement art, larger blast, new event duration or gameplay behavior is introduced. The inclusive boundary matters because the existing late renderer returns at zero progress.

Pinned Godot 4.6.2 rendered 365 addressed native frames across five comparison groups. Inspected early blast, pre-handoff, post-handoff and late frames show retained material under the explosion and the established breakup afterward. All nine bosses and six representative units are included. `verification.json` records exact pixel agreement after handoff in the central artwork regions; labels are excluded from that comparison. Full sequences remain in work, with selected evidence retained here. The native log completed without an engine error or warning.

`baseline.gd`, `candidate.gd`, source binding and `integration.patch` preserve the reviewed change for the later code integration pass. These are presentation fixtures, not natural kills, release acceptance or performance evidence. Production code is unchanged. Final integration still requires natural boss/vehicle destruction captures and the Windows release gate. Large late wrecks overlap the fixture labels slightly; those labels are review annotations, not game UI.

The first comparison fixture lacked clipping between its two rows. Late cruiser spray crossed into the second row and correctly failed pixel agreement at group 0, frame 46. The retained `review_unclipped.gd` and first native log document that attempt. The final fixture uses separate clipped 640x180 Controls and rerenders all groups. Final comparison captures come from `clipped_group_*` under work.

The clipped panels still differ at occasional rasterized pixels when identical rotated artwork is translated vertically (first observed: one pixel in group 0, frame 26). Those raw differences remain in `verification.json`; they are not counted as exact matches. A separate 730-render check draws baseline and candidate sequentially at identical screen coordinates. All 15 cases pass exact post-handoff pixel agreement there, without a relaxed tolerance. See `same_position.gd`, its log and `same_position_checks.json`.
